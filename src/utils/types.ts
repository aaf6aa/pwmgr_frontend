export interface PasswordMetadata {
  id: string;
  service: string;
  username: string;
  timestamp: string;
}

export interface NoteMetadata {
  id: string;
  title: string;
  timestamp: string;
  lastModified: string;
}
